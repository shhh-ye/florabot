"""
Системный промпт — "мозг" бота. Собирается из трёх частей:

1. SYSTEM_PROMPT (ниже) — ядро: кто такой бот и незыблемые правила
   (цены только из таблицы, не выдумывать факты).
2. Скиллы из папки skills/ — постоянные правила поведения, входят в
   КАЖДЫЙ запрос. Один скилл = один .md-файл. Добавить скилл — положить
   файл, убрать — удалить (или переименовать в .md.off, чтобы выключить
   не удаляя). Держите скиллы короткими: чем больше правил, тем хуже
   модель слушает каждое из них.
3. База знаний — факты (доставка, уход, каталог), подгружаются RAG-поиском
   ПО РЕЛЕВАНТНОСТИ вопросу клиента, а не целиком (см. bot/rag/).

Разделение намеренное: правила поведения нужны всегда, факты — только
когда о них спрашивают.
"""

from pathlib import Path

from bot import config

SYSTEM_PROMPT = """
Ты — ИИ-ассистент цветочного магазина, отвечающий клиентам в мессенджерах
(Telegram, Instagram). Твоя роль — вежливый и знающий продавец-консультант.

Правила:
- Отвечай дружелюбно, кратко и по делу, на языке, на котором пишет клиент.
- Помогай подобрать букет под повод и бюджет, рассказывай про доставку,
  оплату и уход за цветами.
- Опирайся на раздел «База знаний» ниже — это выдержки из документов
  магазина, найденные под вопрос клиента. Не выдумывай факты, которых
  там нет.
- ЦЕНЫ И НАЛИЧИЕ: в базе знаний их НЕТ намеренно — они быстро меняются.
  Прежде чем назвать клиенту цену, наличие, остаток или скидку,
  ОБЯЗАТЕЛЬНО вызови инструмент search_google_sheet и возьми цифры из
  его результата. Никогда не называй цену по памяти или из базы знаний.
  Если инструмент товар не нашёл — так и скажи, не подставляй цифру.
- Подбирать варианты (по поводу, стилю, цвету) можно по базе знаний,
  но перед тем как озвучить итоговую цену выбранного товара — проверь
  её инструментом.
- Если ответа нет ни в базе знаний, ни в таблице — честно скажи, что
  уточнишь у менеджера, и предложи оставить контакт.
"""


def load_skills() -> str:
    """
    Склеивает все скиллы из папки skills/ (файлы .md, по алфавиту).

    Файлы с другим расширением (например, переименованные в .md.off)
    не подхватываются — так скилл можно выключить, не удаляя.
    """
    base = Path(config.SKILLS_DIR)
    if not base.is_dir():
        return ""
    parts = []
    for path in sorted(base.glob("*.md")):
        text = path.read_text(encoding="utf-8").strip()
        if text:
            parts.append(text)
    return "\n\n".join(parts)


def build_system_prompt(context: str) -> str:
    """Собирает финальный системный промпт: ядро + скиллы + найденные знания."""
    prompt = SYSTEM_PROMPT

    skills = load_skills()
    if skills:
        prompt += "\n=== Правила поведения (скиллы) ===\n" + skills + "\n"

    if not context:
        return prompt + (
            "\n=== База знаний ===\n"
            "(по этому вопросу ничего не найдено — отвечай осторожно, "
            "не выдумывай конкретику)\n"
        )
    return prompt + "\n=== База знаний ===\n" + context
